[project]
name = "marketplace-bot"
version = "0.1.0"
requires-python = "==3.10.*"

dependencies = [
    "aiogram==3.4.*",
    "sqlalchemy==2.0.*",
    "alembic==1.13.*",
]

[project.scripts]
bot = "marketplace.main:main"
